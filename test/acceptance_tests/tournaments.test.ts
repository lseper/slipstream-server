import * as request from "supertest";

describe("GET /api/v1/tournaments", () => {
	it("SHOULD return 200Ok", (done) => {
		// TODO: need to setup mongoDB instance locally with Docker for mock DB testing
		// request(app)
		// 	.get("/articles")
		// 	.end((err, res) => {
		// 		expect(res.status).toBe(200);
		// 		done();
		// 	});
	});
});
